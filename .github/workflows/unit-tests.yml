name: Unit Tests
on:
  push:
    branches: [main]
jobs:
  unit_tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./npm-expansions
    steps:
      - uses: actions/checkout@v3

      - name: Cargo Test
        run: cargo test